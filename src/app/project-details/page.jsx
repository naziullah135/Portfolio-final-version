import { Box, Container, Typography } from '@mui/material';
import React from 'react';

const page = () => {
    return (
        <Box>
            <Container>
              <Typography variant="h1" color="initial">Project details</Typography>
            </Container>
        </Box>
    );
};

export default page;